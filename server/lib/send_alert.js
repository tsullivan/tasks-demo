// import {
//   ALERTS_INDEX_NAME,
//   ALERTS_INDEX_TYPE,
//   ALERTS_INDEX_MAPPING,
// } from '../../constants';

export async function sendAlert(/* server, hits, params, state */) {
  return true;
}
